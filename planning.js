/* Ethiopian Calendar Planning feature */
(function (global) {
'use strict';
const STORAGE_KEY='ethio-calendar-plans-v1';
const translate=key=>typeof global.t==='function'?global.t(key):(typeof global.__planningTranslate==='function'?global.__planningTranslate(key):key);
function assertPositiveInteger(value,name){const n=Number(value);if(!Number.isInteger(n)||n<=0)throw new Error(`${name} must be a positive integer.`);return n;}
function toJdn(date){if(!date||!Number.isInteger(date.ey)||!Number.isInteger(date.em)||!Number.isInteger(date.ed))throw new Error('Invalid Ethiopian date.');return global.ethiopianToJdn(date.ey,date.em,date.ed);}
function fromJdn(jdn){const e=global.jdnToEthiopian(jdn);return{ey:e.ey,em:e.em,ed:e.ed};}
function addEthiopianMonths(date,months){let total=(date.ey*13)+(date.em-1)+months;const ey=Math.floor(total/13),em=((total%13)+13)%13+1,max=global.getMonthLength(ey,em);return{ey,em,ed:Math.min(date.ed,max)};}
function addEthiopianYears(date,years){const ey=date.ey+years,max=global.getMonthLength(ey,date.em);return{ey,em:date.em,ed:Math.min(date.ed,max)};}
function addUnit(date,value,unit){value=assertPositiveInteger(value,'Interval');switch(unit){case'day':return fromJdn(toJdn(date)+value);case'week':return fromJdn(toJdn(date)+value*7);case'month':return addEthiopianMonths(date,value);case'year':return addEthiopianYears(date,value);default:throw new Error(`Unsupported interval unit: ${unit}`);}}
function compare(a,b){return toJdn(a)-toJdn(b);}
function calculateEndDate(start,periodValue,periodUnit){return addUnit(start,assertPositiveInteger(periodValue,'Period'),periodUnit);}
const SEASON_CATALOG={climatic:[{id:'autumn',labelKey:'season_autumn',label:'Autumn / Kharif'},{id:'summer',labelKey:'season_summer',label:'Summer / Kiremt'},{id:'spring',labelKey:'season_spring',label:'Spring'},{id:'winter',labelKey:'season_winter',label:'Winter'}],fasting:[{id:'none',labelKey:'fast_none',label:'No named fasting season'},{id:'abiy',labelKey:'fast_abiy',label:'Abiy Tsom / Great Lent'},{id:'nebiyat',labelKey:'fast_nebiyat',label:'Fast of the Prophets'},{id:'filseta',labelKey:'fast_filseta',label:'Filseta Fast'},{id:'hawaryat',labelKey:'fast_hawaryat',label:'Hawaryat Fast'},{id:'nenewe',labelKey:'fast_nenewe',label:'Nineveh Fast'},{id:'gehad',labelKey:'fast_gehad',label:'Gehad Fast'},{id:'hamsa',labelKey:'fast_hamsa',label:'Hamsa Elet / Fast season'},{id:'dihnet',labelKey:'fast_dihnet',label:'Dihnet Fast'}],liturgical:['ዘመነ ዮሐንስ','ዘካርያስ','ዘመነ ፍሬ','ዘመነ መስቀል','ዘመነ ጽጌ','ዘመነ አስተምሕሮ','ዘመነ ስብከት','ዘመነ ብርሃን','ዘመነ ኖላዊ','ዘመነ መርዓዊ','አማኑኤል','ዘመነ ልደት','ናዝሬት','ገሐድ','ዘመነ ጥምቀት','ዘመነ ነነዌ','ዘመነ ጾም','ዘመነ ትንሣኤ','ዘመነ ዕርገት','ዘመነ ጰራቅሊጦስ','ደመና፣ ዘርዕ፣ ዝናም','መብረቅ፣ ባሕር','ዐይነ ኵሉ፣ ዕጕለ ቋዓት','ጎሕ፣ ነግሕ'].map(label=>({id:`lit-${label}`,labelKey:label,label}))};
const LENT_WEEK_KEYS=['lent_week_1','lent_week_2','lent_week_3','lent_week_4','lent_week_5','lent_week_6','lent_week_7','lent_week_8'];
function seasonInfoForDate(date){const bh=global.calculateBahreHasab(date.ey),seasons=global.getSeasons(date.ey,date.em,date.ed,bh),greatLentWeek=typeof global.getGreatLentWeek==='function'?global.getGreatLentWeek(global.ethiopianDayOfYear(date.em,date.ed),bh):null;return{climatic:seasons.climatic,fasting:seasons.fasting,fastingProgress:seasons.progress||'',liturgical:seasons.liturgical,greatLentWeek:greatLentWeek?translate(greatLentWeek):''};}
function matchesSeason(info,category,seasonId){if(!category||!seasonId||seasonId==='all')return true;if(category==='climatic'){const item=SEASON_CATALOG.climatic.find(x=>x.id===seasonId);return!!item&&info.climatic===translate(item.labelKey);}if(category==='fasting'){const item=SEASON_CATALOG.fasting.find(x=>x.id===seasonId);if(!item)return false;const label=translate(item.labelKey);return item.id==='none'?info.fasting===label:info.fasting.includes(label);}if(category==='liturgical'){const item=SEASON_CATALOG.liturgical.find(x=>x.id===seasonId||x.label===seasonId);return!!item&&info.liturgical===item.label;}if(category==='lent-week'){const item=LENT_WEEK_KEYS.find(k=>k===seasonId);return!!item&&info.greatLentWeek===translate(item);}return false;}
function getSeasonCatalog(){return{climatic:SEASON_CATALOG.climatic.map(x=>({...x,label:translate(x.labelKey)})),fasting:SEASON_CATALOG.fasting.map(x=>({...x,label:translate(x.labelKey)})),liturgical:SEASON_CATALOG.liturgical.map(x=>({...x})),'lent-week':LENT_WEEK_KEYS.map(k=>({id:k,labelKey:k,label:translate(k)}))};}
function buildSchedule(start,endExclusive,options,periodMeta){const intervalValue=assertPositiveInteger(options.intervalValue,'Interval'),intervalUnit=options.intervalUnit||'day',seasonCategory=options.seasonCategory||'all',seasonId=options.seasonId||'all',rows=[];let current={...start},guard=0,endJdn=toJdn(endExclusive);while(toJdn(current)<endJdn&&guard++<5000){const season=seasonInfoForDate(current);if(matchesSeason(season,seasonCategory,seasonId))rows.push({id:`${current.ey}-${current.em}-${current.ed}`,date:{...current},season,title:'',details:'',status:'planned'});const next=addUnit(current,intervalValue,intervalUnit);if(compare(next,current)<=0)throw new Error('The interval did not advance the schedule.');current=next;}if(guard>=5000&&toJdn(current)<endJdn)throw new Error('The schedule is too large. Reduce the planning period or increase the interval.');return Object.assign({start:{...start},endExclusive:{...endExclusive},intervalValue,intervalUnit,seasonCategory,seasonId,rows},periodMeta);}
function generateSchedule(options){const start={ey:Number(options.start.ey),em:Number(options.start.em),ed:Number(options.start.ed)},periodValue=assertPositiveInteger(options.periodValue,'Period'),periodUnit=options.periodUnit||'month',endExclusive=calculateEndDate(start,periodValue,periodUnit);return buildSchedule(start,endExclusive,options,{periodMode:'duration',periodValue,periodUnit});}
function generateScheduleToDate(options){const start={ey:Number(options.start.ey),em:Number(options.start.em),ed:Number(options.start.ed)},end={ey:Number(options.end.ey),em:Number(options.end.em),ed:Number(options.end.ed)},startJdn=toJdn(start),endJdn=toJdn(end);if(endJdn<startJdn)throw new Error('The end date must be on or after the start date.');return buildSchedule(start,fromJdn(endJdn+1),options,{periodMode:'date-range',periodUnit:'custom',periodValue:1,endDate:{...end}});}
function loadPlans(){try{return JSON.parse(localStorage.getItem(STORAGE_KEY)||'[]');}catch(_){return[];}}
function savePlan(plan){const plans=loadPlans(),index=plans.findIndex(p=>p.id===plan.id);if(index>=0)plans[index]=plan;else plans.push(plan);localStorage.setItem(STORAGE_KEY,JSON.stringify(plans));global.dispatchEvent(new CustomEvent('planning-saved',{detail:{id:plan.id}}));return plan;}
function deletePlan(id){localStorage.setItem(STORAGE_KEY,JSON.stringify(loadPlans().filter(p=>p.id!==id)));global.dispatchEvent(new CustomEvent('planning-saved',{detail:{id}}));}
function csvEscape(value){const s=String(value??'');return /[",\n\r]/.test(s)?`"${s.replace(/"/g,'""')}"`:s;}
function dateLabel(date){return`${date.ey}-${String(date.em).padStart(2,'0')}-${String(date.ed).padStart(2,'0')}`;}
function exportText(plan,format){const rows=plan.rows||[],seasonHeader=plan.seasonCategory&&plan.seasonCategory!=='all'?`Season Filter: ${plan.seasonId}`:'Season Filter: All',periodHeader=plan.periodMode==='date-range'?`Planning Range: ${dateLabel(plan.start)} through ${dateLabel(plan.endDate)}`:`Planning Period: ${plan.periodValue} ${plan.periodUnit}(s)`;if(format==='json')return JSON.stringify(plan,null,2);if(format==='csv')return['Planning Mode,Planning Start,Planning End,Planning Period,Planning Interval,Season Filter',''+[plan.periodMode||'duration',dateLabel(plan.start),plan.endDate?dateLabel(plan.endDate):'',plan.periodValue+' '+plan.periodUnit,plan.intervalValue+' '+plan.intervalUnit,plan.seasonId||'all'].map(csvEscape).join(','),'','Ethiopian Date,Climatic Season,Fasting Season,Liturgical Season,Great Lent Week,Title,Details,Status',...rows.map(r=>[dateLabel(r.date),r.season?.climatic,r.season?.fasting,r.season?.liturgical,r.season?.greatLentWeek,r.title,r.details,r.status].map(csvEscape).join(','))].join('\n');if(format==='tsv')return['Planning Mode\tPlanning Start\tPlanning End\tPlanning Period\tPlanning Interval\tSeason Filter',[plan.periodMode||'duration',dateLabel(plan.start),plan.endDate?dateLabel(plan.endDate):'',plan.periodValue+' '+plan.periodUnit,plan.intervalValue+' '+plan.intervalUnit,plan.seasonId||'all'].map(v=>String(v??'').replace(/[\t\r\n]/g,' ')).join('\t'),'','Ethiopian Date\tClimatic Season\tFasting Season\tLiturgical Season\tGreat Lent Week\tTitle\tDetails\tStatus',...rows.map(r=>[dateLabel(r.date),r.season?.climatic,r.season?.fasting,r.season?.liturgical,r.season?.greatLentWeek,r.title,r.details,r.status].map(v=>String(v??'').replace(/[\t\r\n]/g,' ')).join('\t'))].join('\n');if(format==='md')return[`# ${plan.name||'Ethiopian Plan'}`,periodHeader,seasonHeader,'','| Ethiopian Date | Climatic Season | Fasting Season | Liturgical Season | Great Lent Week | Title | Details | Status |','|---|---|---|---|---|---|---|---|',...rows.map(r=>`| ${dateLabel(r.date)} | ${(r.season?.climatic||'').replace(/\|/g,'\\|')} | ${(r.season?.fasting||'').replace(/\|/g,'\\|')} | ${(r.season?.liturgical||'').replace(/\|/g,'\\|')} | ${(r.season?.greatLentWeek||'').replace(/\|/g,'\\|')} | ${(r.title||'').replace(/\|/g,'\\|')} | ${(r.details||'').replace(/\|/g,'\\|')} | ${r.status} |`)].join('\n');if(format==='html'){const esc=s=>String(s??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));return`<!doctype html><html lang="en"><head><meta charset="utf-8"><title>${esc(plan.name||'Ethiopian Plan')}</title><style>body{font-family:system-ui,sans-serif}table{border-collapse:collapse;width:100%}th,td{border:1px solid #999;padding:.5rem;text-align:left;vertical-align:top}</style></head><body><h1>${esc(plan.name||'Ethiopian Plan')}</h1><p>${esc(periodHeader)}</p><p>${esc(seasonHeader)}</p><table><thead><tr><th>Ethiopian Date</th><th>Climatic Season</th><th>Fasting Season</th><th>Liturgical Season</th><th>Great Lent Week</th><th>Title</th><th>Details</th><th>Status</th></tr></thead><tbody>${rows.map(r=>`<tr><td>${esc(dateLabel(r.date))}</td><td>${esc(r.season?.climatic)}</td><td>${esc(r.season?.fasting)}</td><td>${esc(r.season?.liturgical)}</td><td>${esc(r.season?.greatLentWeek)}</td><td>${esc(r.title)}</td><td>${esc(r.details)}</td><td>${esc(r.status)}</td></tr>`).join('')}</tbody></table></body></html>`;}throw new Error(`Unsupported export format: ${format}`);}
function downloadPlan(plan,format){const extensions={json:'json',csv:'csv',tsv:'tsv',md:'md',html:'html'},mime={json:'application/json',csv:'text/csv',tsv:'text/tab-separated-values',md:'text/markdown',html:'text/html'},blob=new Blob([exportText(plan,format)],{type:mime[format]||'text/plain'}),a=document.createElement('a');a.href=URL.createObjectURL(blob);a.download=`${(plan.name||'ethiopian-plan').replace(/[^\p{L}\p{N}_-]+/gu,'_')}.${extensions[format]||'txt'}`;document.body.appendChild(a);a.click();a.remove();setTimeout(()=>URL.revokeObjectURL(a.href),0);}
function parseDelimited(text,delimiter){const rows=[];let row=[],field='',quoted=false;for(let i=0;i<text.length;i++){const c=text[i],n=text[i+1];if(quoted){if(c==='"'&&n==='"'){field+='"';i++;}else if(c==='"')quoted=false;else field+=c;}else if(c==='"')quoted=true;else if(c===delimiter){row.push(field);field='';}else if(c==='\n'){row.push(field);rows.push(row);row=[];field='';}else if(c!=='\r')field+=c;}if(field!==''||row.length){row.push(field);rows.push(row);}return rows;}
function parseImportDate(value){const m=String(value||'').trim().match(/^(\d+)-(\d{1,2})-(\d{1,2})$/);if(!m)throw new Error('Invalid Ethiopian date.');const d={ey:+m[1],em:+m[2],ed:+m[3]};toJdn(d);return d;}
function importJson(text){const p=JSON.parse(text);if(!p||!p.start||!Array.isArray(p.rows))throw new Error('Invalid planning JSON.');p.start=parseImportDate(dateLabel(p.start));if(p.endDate)p.endDate=parseImportDate(dateLabel(p.endDate));p.rows=p.rows.map(r=>({...r,date:parseImportDate(dateLabel(r.date)),title:String(r.title||''),details:String(r.details||''),status:r.status||'planned'}));p.id=`imported-${Date.now()}`;return p;}
function importDelimited(text,delimiter){const rows=parseDelimited(text,delimiter),header=rows.findIndex(r=>r.some(x=>String(x).trim()==='Ethiopian Date'));if(header<0||rows.length<header+2)throw new Error('Missing planning table.');const h=rows[header].map(x=>String(x).trim()),idx=n=>h.indexOf(n),di=idx('Ethiopian Date');if(di<0)throw new Error('Missing Ethiopian Date column.');const meta=rows[1]||[],start=parseImportDate(meta[1]);const end=meta[2]?parseImportDate(meta[2]):null;const pp=String(meta[3]||'1 day').trim().split(/\s+/),iv=String(meta[4]||'1 day').trim().split(/\s+/),p={id:`imported-${Date.now()}`,name:'Imported Ethiopian Plan',periodMode:meta[0]||'duration',start,periodValue:Number(pp[0])||1,periodUnit:pp[1]||'day',intervalValue:Number(iv[0])||1,intervalUnit:iv[1]||'day',seasonCategory:'all',seasonId:meta[5]||'all',rows:[]};if(end){p.endDate=end;p.periodUnit='custom';p.endExclusive=addUnit(end,1,'day');}for(let i=header+1;i<rows.length;i++){const r=rows[i];if(!r[di])continue;const d=parseImportDate(r[di]);p.rows.push({id:`${d.ey}-${d.em}-${d.ed}`,date:d,season:{climatic:r[idx('Climatic Season')]||'',fasting:r[idx('Fasting Season')]||'',liturgical:r[idx('Liturgical Season')]||'',greatLentWeek:r[idx('Great Lent Week')]||''},title:idx('Title')>=0?r[idx('Title')]||'':'',details:idx('Details')>=0?r[idx('Details')]||'':'',status:idx('Status')>=0&&r[idx('Status')]?r[idx('Status')]:'planned'});}if(!p.rows.length)throw new Error('No planning rows found.');return p;}
function importPlanText(text,format){if(format==='json')return importJson(text);return importDelimited(text,format==='tsv'?'\t':',');}
function injectFileTools(){if(!document.getElementById('tab-planning')||document.getElementById('planning-file-tools'))return;const host=document.querySelector('#tab-planning .planning-card');if(!host)return;const box=document.createElement('section');box.id='planning-file-tools';box.setAttribute('aria-labelledby','planning-file-tools-title');box.innerHTML='<h3 id="planning-file-tools-title">Planning files</h3><div class="planning-actions"><button type="button" id="planning-blank-form">Download blank form</button><button type="button" id="planning-filled-form">Download filled plan</button><button type="button" id="planning-import-file">Import plan</button><input id="planning-file-input" type="file" accept=".json,.csv,.tsv" hidden><span id="planning-file-status" role="status" aria-live="polite"></span></div>';host.appendChild(box);const current=()=>global.__planningCurrentPlan||global.__importedPlanningPlan||null;document.getElementById('planning-blank-form').onclick=()=>{const p=current();if(!p)return;const blank=JSON.parse(JSON.stringify(p));blank.rows=(blank.rows||[]).map(r=>({...r,title:'',details:'',status:'planned'}));downloadPlan(blank,'csv');};document.getElementById('planning-filled-form').onclick=()=>{const p=current();if(p)downloadPlan(p,'json');};document.getElementById('planning-import-file').onclick=()=>document.getElementById('planning-file-input').click();document.getElementById('planning-file-input').onchange=async e=>{const f=e.target.files[0];if(!f)return;try{const p=importPlanText(await f.text(),/\.json$/i.test(f.name)?'json':/\.tsv$/i.test(f.name)?'tsv':'csv');savePlan(p);global.__importedPlanningPlan=p;document.getElementById('planning-file-status').textContent='Plan imported.';}catch(err){document.getElementById('planning-file-status').textContent=err.message;}e.target.value='';};}
function watch(){injectFileTools();setTimeout(watch,500);}watch();
global.EthioPlanner={addUnit,calculateEndDate,generateSchedule,generateScheduleToDate,seasonInfoForDate,matchesSeason,getSeasonCatalog,loadPlans,savePlan,deletePlan,exportText,downloadPlan,importPlanText};
})(window);